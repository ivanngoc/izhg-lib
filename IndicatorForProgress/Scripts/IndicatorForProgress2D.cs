using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using IziHardGames;
// using UnityEngine.UI;

namespace IziHardGames.Libs.Engine.IndicatorForProgress
{
	public class IndicatorForProgress2D : MonoBehaviour
	{
		// [SerializeField] Image image;
		// [SerializeField] Text title;
		// [SerializeField] float timeToCountdown;

		// public float timeCurrent;
		// public int counter;

		// #region Unity Message
		// // Start is called before the first frame update
		// void Start()
		// {

		// }

		// // Update is called once per frame
		// void Update()
		// {
		// 	if (timeToCountdown > 0)
		// 	{
		// 		timeCurrent -= Time.deltaTime;
		// 	}
		// 	var value = (1f - timeCurrent / timeToCountdown);

		// 	if (float.IsNormal(value) || value == 0f)
		// 	{
		// 		image.fillAmount = value;
		// 	}

		// 	if (timeCurrent < 0)
		// 	{
		// 		timeCurrent = timeToCountdown;
		// 		counter++;
		// 		title.text = counter.ToString();
		// 	}
		// }
		// #endregion
	}
}