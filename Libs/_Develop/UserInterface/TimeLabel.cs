using System;
using UnityEngine;
// using UnityEngine.UI;

namespace IziHardGames.Develop.UI
{
	public class TimeLabel : MonoBehaviour
	{
		// [SerializeField] Text text;

		// #region Unity Message
		// private void LateUpdate()
		// {
		// 	UpdateLabel();
		// }
		// #endregion

		// public void UpdateLabel()
		// {
		// 	text.text = $"FPS: {Application.targetFrameRate.ToString()}|{((int)(1f / Time.unscaledDeltaTime)).ToString()}	unscaleDT=[{Time.unscaledDeltaTime.ToString("F2")}]	scale=[{Time.timeScale.ToString("F2")}]	[{Time.frameCount.ToString()}]	{DateTime.Now.ToString()}";
		// }
	}
}